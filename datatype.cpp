#include <iostream>
using namespace std;

int main()
{

    /*
    cout << 4;
    cout << 5;
    cout << 7;
    cout << endl;
    */
    // cout << 4 << 5 << 7 << endl;

    // cout << 4;
    // cout << "4";

    // sanjana ki age jaante hai

    // age store krne k liye memory chahiye
    // variable declaration
    //  int age;

    // cout << "Enter your age Sanjana: ";
    // cin >> age;

    // cout << "Your age is: " << age << endl;

    // variable decalaration
    // ek integer type ka variable banado jiska naame age hai
    // int age;

    // variable initialisation
    // int age = 20;
    // integer type k age variable k andar 20 value assign kardo

    // char ch = 'a';
    //  char ch2 = 'z';
    //  cout << ch2 << endl;

    // float f = 11.6;
    // double d = 12.34634324634;

    // char ch = 'l';
    // cout << sizeof(ch) << endl;

    // char ch = 190;
    // cout << "Character is: " << ch << endl;

    // short value = 32770;     
    //Range for short datatype is -32768 to 32767 so when we give value beyond the range of a datatype then it wraps itself.
    //Wraps itself like max is 32767 and we are giving +3 beyond its range- So 32767 then goes to -32768(this is +1), now -32767(this is +2) and at last it is -32766, which is +3.
    //That's why we get -32766 as output.
    // cout << "value is: " << value << endl;

    // bool found = 0;
    // cout << found << endl;

    float num=344354564.6576;
    cout<<(num)<<endl;

    return 0;
}